apiVersion: v1
kind: Service
metadata:
  name: devops-api
  namespace: devops
  labels:
    app: devops-api
spec:
  type: NodePort
  selector:
    app: devops-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
      nodePort: 30080
